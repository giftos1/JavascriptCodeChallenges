// the function takes the age in years and returns the age in days
// use 364 days as length of years
// ignore leap years and days between last birthday and now.

function calcAge(age) {
    return age * 364;
}

console.log(calcAge(10));